Micrograd is an Autograd (Automatic Gradient) Engine which basically implements backpropagation.
Backpropagation is not exclusively a neural network algorithm; it is a general mathematical technique for computing exact gradients across any chain of differentiable mathematical functions.

Actual backpropagation and neural network code is just 150 lines, rest is just efficiency.

__repr__ defines the string representation of an object, mainly intended for debugging and developers.

At the end, I now have a revision on backpropagation, and how to implement Neural Networks from scratch.